start
Select kamus stopword dari database
if (stopword)
	if (result_text in stopword)
		Hapus result_text (stopword) dengan kata asli
		Output result_text
		Simpan result_text
	else
		stopword selanjutnya
		Kembali ke nomor 3
	endif
endif
end