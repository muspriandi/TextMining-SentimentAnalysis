start
Select kamus stopword dari database
if (stopword != null)
	if (result_text in stopword)
		Hapus result_text (stopword)
		Output result_text
		Simpan result_text
	endif
	stopword selanjutnya
	Kembali ke nomor 3
endif
end