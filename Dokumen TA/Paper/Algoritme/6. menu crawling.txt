Select data crawling
Tampilkan halaman crawling
aksi = ''
pilih = ''
if (aksi == 'crawling data')
	Akses proses crawling
	Tampil hasil proses crawling
	if (pilih == 'kembali')
		Kembali ke nomor 1
	endif
else if	(aksi == 'import data')
	Tampil modal import data
	Input field file
	if (pilih == 'import')
		Insert database
		Kembali ke nomor 1
	else
		Tutup modal import data
		Kembali ke nomor 1
	endif
else
	Akses menu awal
endif