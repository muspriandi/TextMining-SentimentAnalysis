start
Select kamus slangword dari database
if (slangword != null)
	if (teks == slangword)
		Ubah result_text (slangword) dengan kata asli
		Output result_text
		Simpan result_text 
	else 
		slangword selanjutnya
		Kembali ke nomor 3
	endif
endif
end