Select data model
Tampilkan halaman modeling
aksi = ''
pilih = ''
if (aksi == 'modeling data')
	Akses proses modeling
	Tampil hasil proses modeling
	if (pilih == 'kembali')
		Kembali ke nomor 1
	endif
else if	(aksi == 'hapus record data')
	Tampil modal hapus record data
	if (pilih == 'hapus')
		Delete record database, file
		Kembali ke nomor 1
	else
		Tutup modal hapus record data
		Kembali ke nomor 1
	endif
else
	Akses menu awal
endif